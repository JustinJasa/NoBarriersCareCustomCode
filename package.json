{
  "name": "nobarrierscarecustomcode",
  "version": "1.0.0",
  "description": "Animation Code Environments for Client Webflow Sites",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel script.js", 
    "build": "npx parcel build script.js --dist-dir dist --no-content-hash"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gsap": "^3.13.0",
    "install": "^0.13.0"
  },
  "devDependencies": {
    "parcel": "^2.15.2"
  }
}
